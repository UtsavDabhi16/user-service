#spring:
#  config:
#    import: optional:config.server:http://localhost:9006/
#    enabled: true
#    fail-fast: true

spring:
  cloud:
    config:
      uri:
        - http://localhost:9006

management:
  endpoints:
    web:
      exposure:
        include: "*"

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/